# node-modules rather than PnP: this package publishes a CLI that end users
# install with plain npm, so PnP would only add friction during development.
# Switch to `nodeLinker: pnp` if you prefer it — nothing in src depends on it.
nodeLinker: node-modules

enableGlobalCache: true
