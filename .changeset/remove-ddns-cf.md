---
---

Remove the private `ddns-cf` app. Its webhook chat-proxy moved to the vs-grum repo as a standalone management bot, and its Cloudflare DDNS role is covered by the published `@signalbox/cloudflare-ddns`.
