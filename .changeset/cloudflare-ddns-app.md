---
"@signalbox/cloudflare-ddns": minor
---

Publish the Cloudflare DDNS reference app. A config-driven CLI (via `@signalbox/service-cli`) with a systemd lifecycle that keeps Cloudflare A records pointed at a home connection's public IP, driven by UPnP push and a polling fallback.
